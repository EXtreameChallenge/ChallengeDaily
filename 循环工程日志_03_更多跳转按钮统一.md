# 第 3 轮循环工程日志

## 本次循环对应子目标：Sub-Goal 2 - 首页全模块统一新增「更多」跳转按钮并修复跳转链路

### 1. 目标对齐确认
- 核心需求点：
  1. 首页每个独立功能模块右上角统一新增跳转按钮，命名统一为「更多」。
  2. 每个按钮点击后准确跳转到对应独立详情页。
  3. 排查现有跳转功能，修复点击无响应、路径错误、空白页面问题。
  4. 跳转交互遵循行业规范：hover 有反馈、点击有加载状态、详情页可返回首页。
- 验收通过标准：
  - 首页所有模块右上角显示「更多」按钮。
  - 点击后跳转到正确详情页。
  - hover 有箭头位移+颜色变化反馈。
  - 详情页可通过侧边栏返回首页。
- 对标参考项目：Ant Design Pro、Vue Admin 的模块跳转交互设计。

### 2. 对标调研记录
- 调研对象：
  - 原有实现：`CardHeader` 组件文案为"查看详情"，已有 hover 箭头反馈。
  - Ant Design Pro：卡片右上角"更多"按钮 + 箭头图标，hover 高亮。
- 核心实现原理/设计亮点提炼：
  - 统一组件 `CardHeader` 确保所有模块视觉一致。
  - hover 时箭头从隐藏到显现+位移，提供交互反馈。
- 适配本项目的落地方案：
  - 仅改文案"查看详情"→"更多"，交互逻辑已满足需求。
  - 排查路由定义，确认 /timeline、/heatmap、/apps 均有效。

### 3. 开发落地详情
- 修改/新增的文件清单：
  - `client/src/pages/Overview.tsx`（CardHeader 文案统一）
  - `循环工程日志_03_更多跳转按钮统一.md`（本日志）
- 核心实现逻辑说明：
  - CardHeader 组件文案从"查看详情"改为"更多"。
  - 路由排查：App.tsx 定义了 /timeline、/heatmap、/apps 等全部有效路由。
- 功能联调验证结果：
  - 6 个模块（时间分布、热力图、应用使用、效率趋势、个人节奏、最近活动）均显示"更多"。
  - 跳转目标：/timeline、/heatmap、/apps 均有对应页面组件。
  - 详情页通过侧边栏可返回首页。

### 4. 自检缺陷清单与修复记录
| 序号 | 缺陷描述 | 是否修复 | 修复方案 | 验证结果 |
|------|----------|----------|----------|----------|
| 1 | 按钮文案不统一为"更多" | 是 | CardHeader 文案改为"更多" | 全部模块统一 |
| 2 | 跳转链路有效性排查 | 无缺陷 | /timeline、/heatmap、/apps 路由均存在 | 全部有效 |
| 3 | Heatmap.tsx 和 HistoryReports.tsx 中的"查看详情"为描述性文字 | 不需改 | 非跳转按钮，保留原文 | - |

### 5. 本子目标完成进度：100%
### 6. 下一轮循环计划
进入 Sub-Goal 3：AI 超长上下文能力升级（对标 MIMOCDE 实现长记忆机制）。
