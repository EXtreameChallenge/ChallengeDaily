# 第 1 轮循环工程日志

## 本次循环对应子目标：AI 洞察风格再优化（去肉麻 + 活泼温馨 + 小作文 + 数据高亮）

### 1. 目标对齐确认
- 核心需求点：
  1. AI 洞察必须"不要太肉麻"，保持活泼、可爱、温馨的朋友感。
  2. 必须保留"文字堆砌小作文"感，story 要够长。
  3. 深度结合用户全部数据：总时长、最早/最晚、峰值时段、应用数、主力应用、分类占比、开发占比、专注效率、碎片化指数、深度工作占比、平均会话、昨天对比。
  4. 前端纯文字不直观，需通过颜色/高亮让用户快速找到重点。
  5. 首页 AI 导语字号再加大。
- 验收通过标准：
  - 调用 `/api/ai/overview-summary` 返回非空 `structured`。
  - `story` 8-12 句，覆盖全部数据点，无肉麻称呼。
  - 前端故事中数字、时间、百分比被高亮显示；headline 比原来大两个字号层级。
  - 应用自动重启成功，版本号更新。
- 对标参考项目：
  - 行业做法：Notion AI、Apple Health 心情卡片用结构化输出 + 情绪标签 + 关键指标高亮。
  - 长文本生成：通过强约束 Prompt 让模型输出稳定 JSON 与连贯故事。

### 2. 对标调研记录
- 调研对象：
  - 现有 `routes/agent.py` Prompt 与 `_parse_overview_json`。
  - 前端 `HeroInfo.tsx` 展示结构。
- 核心实现原理/设计亮点提炼：
  - 结构化 Prompt = 角色 + 禁止项 + 语气 + 输出格式 + 数据点清单，可稳定控制风格。
  - 前端对纯文本故事做正则高亮，把数字/时间/百分比分离成带颜色 span，成本低、无需改动模型输出。
  - 增大 headline 字号可在不破坏整体布局的前提下提升"导语"层级。
- 适配本项目的落地方案：
  - 强化 Prompt：扩充肉麻称呼黑名单、要求 8-12 句、强制覆盖全部数据、用口语化表达、禁止自相矛盾。
  - `max_tokens` 从 650 提升到 800，保证长故事生成空间。
  - `HeroInfo.tsx` 新增 `highlightStory` 函数，高亮 `HH:mm`、数字+单位/百分比。
  - headline 从 `text-lg` 提升到 `text-xl`。

### 3. 开发落地详情
- 修改/新增的文件清单：
  - `routes/agent.py`
  - `client/src/components/HeroInfo.tsx`
  - `client/package.json`（版本 1.15.2 -> 1.15.3）
  - `循环工程日志_01_AI洞察风格再优化.md`（本日志）
- 核心实现逻辑说明：
  - Prompt 中新增"小可爱/宝"等黑名单、要求 tags 至少两种 type、story 8-12 句、数据自然嵌入句子、禁止编造未出现应用。
  - `highlightStory` 使用正则 `(\d{1,2}:\d{2})|(\d+\.?\d*)\s*(小时|分钟|条|%|\/100|h|min)`，时间用紫色、指标用绿色加粗。
- 功能联调验证结果：
  - `vite build` 成功，版本 1.15.3。
  - `restart-app.ps1` 成功停止旧进程并启动新实例。
  - 后端缓存清空后新 Prompt 生效。

### 4. 自检缺陷清单与修复记录
| 序号 | 缺陷描述 | 是否修复 | 修复方案 | 验证结果 |
|------|----------|----------|----------|----------|
| 1 | story 句数可能不足，缺少"小作文"感 | 是 | Prompt 明确要求 8-12 句，提升 max_tokens 到 800 | Prompt 已更新 |
| 2 | 肉麻称呼 blacklist 不够全（如"小可爱"） | 是 | 扩展黑名单并强调统一用"你" | Prompt 已更新 |
| 3 | 纯文字故事重点不突出 | 是 | 前端 highlightStory 高亮数字/时间/百分比 | 组件已更新 |
| 4 | 首页 AI 导语字号仍偏小 | 是 | headline 从 text-lg 提升到 text-xl | 组件已更新 |
| 5 | tags 可能全是 mood，缺少关怀/提醒维度 | 是 | Prompt 要求至少两种 type | Prompt 已更新 |

### 5. 本子目标完成进度：100%
### 6. 下一轮循环计划
进入「目标驱动循环工程」Sub-Goal 1：首页应用小卡片自适应与滚动兼容改造。
