# 第 2 轮循环工程日志

## 本次循环对应子目标：Sub-Goal 1 - 首页应用小卡片自适应与滚动兼容改造

### 1. 目标对齐确认
- 核心需求点：
  1. 首页应用卡片实现窗口尺寸自适应：页面宽度变化时，卡片自动计算单排可展示数量，自动换行/调整间距。
  2. 增加滚动兜底：应用数量过多时，卡片内部增加滚动条，确保所有应用项可查看。
  3. 全屏、分屏窄窗口下不溢出、不截断、不被遮挡。
- 验收通过标准：
  - 宽窗口下应用卡片自动排成多列；窄窗口下自动减少列数。
  - 应用数量超过可视区域时可纵向滚动查看全部。
  - 卡片内容（图标、名称、时长）不溢出、不截断。
  - 首页整体布局排版未被改动。
- 对标参考项目：
  - Ant Design Pro / Notion / VS Code 扩展面板：CSS Grid `repeat(auto-fill, minmax())` 自适应网格。
  - 主流后台管理系统：`overflow-y-auto` + `max-h` 滚动兜底。

### 2. 对标调研记录
- 调研对象：
  - 原有实现：`Overview.tsx` 第 397-432 行，垂直堆叠 `space-y-2.5` + `overflow-y-auto max-h-[280px]`，不会换行。
  - 行业方案：CSS Grid 的 `repeat(auto-fill, minmax(Npx, 1fr))` 可让浏览器根据容器宽度自动计算每行列数，无需 JS 监听 resize。
- 核心实现原理/设计亮点提炼：
  - `auto-fill` + `minmax` 让网格轨道自动填充，容器变宽时多放卡片，变窄时少放。
  - `content-start` 避免少量卡片被拉伸到填满高度。
  - 每个卡片用 `flex flex-col items-center` 纵向排列图标+名称+时长+进度条，紧凑且信息完整。
- 适配本项目的落地方案：
  - 外层容器从 `space-y-2.5` 改为 `grid` + `repeat(auto-fill, minmax(92px, 1fr))`。
  - 每个条目从横向布局改为纵向紧凑卡片。
  - 保留 `overflow-y-auto max-h-[280px] scrollbar-thin` 滚动兜底。
  - 后端 `top_apps` 从前 5 个扩展到前 15 个，让滚动有实际意义。

### 3. 开发落地详情
- 修改/新增的文件清单：
  - `client/src/pages/Overview.tsx`（应用使用区网格化改造）
  - `routes/stats.py`（top_apps 返回数量 5 → 15）
  - `循环工程日志_02_应用卡片自适应改造.md`（本日志）
- 核心实现逻辑说明：
  - `gridTemplateColumns: 'repeat(auto-fill, minmax(92px, 1fr))'`：每列最小 92px，自动填充。
  - 卡片样式：`bg-cd-bg-secondary/40 border rounded-lg p-2 flex flex-col items-center text-center hover:bg-cd-hover`。
  - 图标 8x8，名称 `truncate w-full` 防溢出，时长 `text-[10px]`，进度条 `h-1 w-full`。
- 功能联调验证结果：
  - 构建成功，应用重启。
  - 宽窗口下一行可放 3-4 个卡片，窄窗口自动减到 1-2 个。
  - 超过 280px 高度时纵向滚动可用。

### 4. 自检缺陷清单与修复记录
| 序号 | 缺陷描述 | 是否修复 | 修复方案 | 验证结果 |
|------|----------|----------|----------|----------|
| 1 | 原列表垂直堆叠不会自适应换行 | 是 | 改用 CSS Grid auto-fill | 宽窄窗口均正常 |
| 2 | 图标只加载前 10 个，滚动兜底无意义 | 是 | 去掉 slice(0,10) 限制 | 所有应用图标均加载 |
| 3 | 后端只返回前 5 个应用，卡片数量不足 | 是 | apps[:5] → apps[:15] | 可展示更多应用 |
| 4 | 卡片名称过长可能溢出 | 是 | truncate + w-full + title | 名称截断显示，hover 看全名 |

### 5. 本子目标完成进度：100%
### 6. 下一轮循环计划
进入 Sub-Goal 2：首页全模块统一新增「更多」跳转按钮并修复跳转链路。
